def create_module(skill):
    return {
        "module": f"{skill} for Production",
        "duration": "2 weeks",
        "labs": [
            f"Hands-on {skill} pipeline"
        ],
        "project": f"Build real-world {skill} system"
    }
